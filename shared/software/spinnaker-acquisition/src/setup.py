#!/usr/bin/env python

from setuptools import setup

setup(name='spinnaker-acquisition',
      version='1.0',
      # Modules to import from other scripts:
      # packages=find_packages(),
      # Executables
      scripts=["Acquisition.py"],
     )